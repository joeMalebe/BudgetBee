package za.co.app.budgetbee.ui.views

import android.content.Context
import androidx.constraintlayout.widget.ConstraintLayout

class Switcher(context: Context) : ConstraintLayout(context) {

    constructor(context: Context, switcherValues: ArrayList<String>) : this(context)
}